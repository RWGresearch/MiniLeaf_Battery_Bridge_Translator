# Manual Verification Checklist

**Working document, not a finished reference.** Mirrors
`Refrance/Leaf_BMS_Emulator/battery emulator overview/10-manual-verification-checklist.md` — a
per-feature checklist for going back through what's been personally tested against real hardware
(RZ450e bench pack + real Leaf car) vs. what's only documented/researched. Fill this in as testing
happens; findings get folded back into `05-battery-management-safety.md` (promoting a researched
default to "user-confirmed," correcting anything wrong, flagging untested items as still open) —
same discipline both reference projects already use.

Status values: **Documented** (researched/inherited, not yet tested on this specific setup),
**Needs Validation** (tested once, result unclear or contradicts the default), **Confirmed** (user
personally tested and signed off).

## Battery management features (`05-battery-management-safety.md`)

| Feature | Default | Status | Notes |
|---|---|---|---|
| Low-voltage cutoff (3.00V, `capacity_empty`), cell-voltage authoritative | 3.00V/cell | Confirmed (software) | **Safety bug found and fixed 2026-07-31**: SoC used to be OR'd in as an equal independent trigger, so a low SoC reading alone could cut off the pack even with every cell healthy. Reproduced directly (3.50V cells + 5% SoC → incorrectly cut off before the fix) and confirmed fixed (same scenario now stays 0, with a visible "SoC backup check does not confirm" warning instead of acting). Also verified the reverse (low cells + healthy SoC still correctly cuts off) and the agreement case. Still needs a real discharge test down near 3.00V/cell to confirm the threshold itself, separate from the trigger-logic fix. |
| Low-voltage soft-cut persistence window (2.0s) | 2.0s | Confirmed (software) | **Added 2026-07-31** (docs/12 finding F5). Unit-tested (`tests/test_management_engine.py`): a cell held at 2.90V does NOT latch `capacity_empty` on the first tick, but does latch once the condition has persisted past 2.0s; a cell at 2.70V (below the emergency floor) triggers the hard cut instantly with no persistence delay, confirming the two tiers behave differently as designed. Still needs a real spike-load test (hard acceleration on a marginal pack) to confirm 2.0s is the right real-world window - too short doesn't guard against real sag transients, too long delays a genuine cutoff. |
| Emergency low-voltage (2.60V, hard cut) | 2.60V/cell | Documented | Value lowered from 2.80V (user edit, 2026-08-01). Not yet tested. |
| Charge/regen power limit (`charge_target_taper`, proactive 4.00V full → 4.15V zero, 4.20V emergency, regen only) | 4.00/4.15/4.20V/cell | Documented | **Corrected 2026-07-31, split from AC charging 2026-08-01, emergency tier tightened 2026-08-03** (was 4.30V, now the standard NMC ceiling exactly): taper driven purely by continuous per-cell voltage (not gated by SoC), doubles as the regen-braking power limit while driving (`charge_limit_kw` is the Leaf's shared "Charge/regen power limit" field), active regardless of charging context. Curve widened from an original 3.90/4.10V test to the current 4.00/4.15V default when the AC-charger taper was split into its own feature. Still needs a real regen event (e.g. descending a hill near-full) to confirm the curve is actually wide/early enough against the real VCM's response lag. |
| AC charge target + taper (`ac_charge_taper`, 4.00V full → 4.15V min (holds) → 4.18V cutoff (stops) → 4.20V emergency, 80%/100% SoC stop) | 4.00/4.15/4.18/4.20V/cell, 80/100% | Documented | **Split into its own feature 2026-08-01** (was combined with the row above), **emergency tier tightened 2026-08-03** (was 4.30V, matching the regen-side change above), **reworked 2026-08-06, twice the same day** (min-kW floor instead of true zero, new explicit `ac_cutoff_v` stop, then a dynamically-selected 0-7 convergence rate replacing an initial fixed-hysteresis attempt - see `05-battery-management-safety.md`'s "AC charger taper rework" section). Root cause of the 2026-08-05 log's oscillation initially mis-traced against the CODE DEFAULT `ac_full_v`/`ac_min_v` (the log's voltage never reaches 4.00-4.15V) - the user confirmed the actual test used a deliberately bracketed 3.62V/3.64V window instead (`docs/13`'s "bracket the threshold" technique), which the log's voltage sits squarely inside; re-checked with the correct values and the OLD zero-hysteresis formula reproduces the log's observed `charger_limit_kw` to within encoding rounding at every sample checked (`docs/13` item 17.6) - diagnosis fully confirmed, not just plausible. |
| AC taper min-kW floor / cutoff voltage / dynamic convergence rate (`ac_min_kw`, `ac_cutoff_v`, `_AC_LEVEL_DOWNSHIFT_KW`, added 2026-08-06) | 0.5kW / 4.18V / 3.0-0.05kW thresholds (levels 7-1) | Documented | Replayed the REAL captured cell-voltage trace from the 2026-08-05 log through the current algorithm with the actual bracketed `ac_full_v=3.62V`/`ac_min_v=3.64V` (`tests/check_ac_taper_log_replay.py`, new) - worst single-call jump across the entire ~1780s real replay is 0.819kW, vs. the original real multi-kW same-tick jumps. Strong real-data evidence, but this is a software replay of captured data, not a live real-hardware retest - still needs an actual bench session confirming the taper holds at `ac_min_kw`, `ac_cutoff_v` ends the session (`full_charge_flag`) correctly, and the transmitted 0x1DC uprate bits visibly track the selected level while converging (`docs/15` B20) before promoting to Confirmed. |
| AC/DC charge power request bounds (`ac_min_kw`/`ac_max_kw`/`dc_min_kw`/`dc_max_kw`, added 2026-08-06) | 0.5/6.6 kW (AC), 5/50 kW (DC placeholder) | Documented | AC bounds now clamp both the manual ramp target and the AC taper's floor - not yet confirmed the clamp behaves correctly at both extremes on real hardware. DC fields are a placeholder only, not wired to any logic - nothing to verify there yet. |
| Charge ramp fine-precision fix (symmetric rate-limited ramp, both directions) | N/A (bug fix) | Documented (software-verified) | **Bug found and fixed 2026-08-06**: `RealtimeEngine._apply_charge_ramp()` previously only rate-limited an INCREASING ramp target (`min(current + rate*dt, target)`); a DECREASING target snapped instantly with no rate limit, confirmed directly in the 2026-08-05 bench log (`charger_limit_kw` jumping 5.20kW→5.00kW in a single 10ms tick). Now rate-limited symmetrically in both directions. Not yet re-tested on real hardware. |
| `full_charge_flag` charging-context gate (`charge_permission_input`) | gated, off by default | Confirmed (software) | **Safety bug found and fixed 2026-07-31**: `full_charge_flag` (instant contactor drop on the real Leaf) previously fired from SoC alone with no charging-context check, which would have dropped HV contactors while simply driving above the target SoC. Reproduced directly (85% SoC, not charging → `full_charge_flag` incorrectly set before the fix) and confirmed fixed (same scenario now stays 0; only fires when `charge_permission_input` is active). Verifiable in software alone - re-verify once running against a real charge session that a legitimate stop still fires correctly. |
| Over-temperature derate, restructured (cold-derate 10/0°C on coldest probe, charge 32/45°C, discharge 55/60°C on hottest probe, all soft) | see `05-...md` | Documented (bug fix confirmed in software) | **Restructured 2026-07-31** (docs/12 findings F1/F3/F6). **Bug fixed and confirmed** (`tests/test_management_engine.py`): the cold-side charge block previously tested the hottest probe, so a pack with a frozen coldest cell but a warm hottest cell would still accept charge - unit-tested directly (hottest 70°F / coldest 20°F → charge correctly blocked; the reverse scenario → charge correctly NOT blocked) and confirmed fixed. Added a cold-side derate ramp (10°C→0°C) unit-tested at the midpoint (41°F → ~50% factor). General NMC research, not RZ450e-specific — the temperature values themselves still need real thermal testing. |
| Emergency temp (61°C/141.8°F, hard cut, hottest probe) | 141.8°F | Confirmed (software) | **Added 2026-07-31** (docs/12 finding F6), **tightened 2026-08-01** (was 65°C/149°F) - previously, hitting the soft `discharge_hard_stop_f`/`charge_hard_stop_f` values directly asserted a hard cut, with no separate emergency tier (inconsistent with this feature's own "Soft (ramp)" documentation). Unit-tested: at exactly 140°F (the soft hard-stop), discharge power now correctly reaches zero WITHOUT a hard cut firing; above the 141.8°F emergency tier, a hard cut correctly fires. Value itself is researched/user-tightened, not yet tested against a real thermal event on this pack. |
| Staleness watchdog (60s soft, +5s hard) | 60s / 65s | Documented | Needs a deliberate CAN-disconnect test to confirm the escalation actually fires as designed. Soft stage also now sets `full_charge_flag=1` in addition to `capacity_empty`/zeroed power limits (added 2026-08-03) — not yet observed on real hardware whether this specific side effect is desirable during an active charge session. |
| Discharge power taper (proactive 3.00V full → 2.60V zero, fast-attack/slow-release hysteresis) | 3.00/2.60V/cell, 3.0s recovery | Documented | **Reworked 2026-07-31, re-anchored 2026-08-01** (was 3.50/3.00V): now per-cell-voltage-driven (not SoC) with fast-attack/slow-release hysteresis, mirroring the charge/regen taper. Unit-tested the curve and the hysteresis directly (a sudden dip snaps discharge power to 0 instantly; recovering climbs back to full only gradually over the configured recovery-ramp time). Still needs a real discharge test under actual acceleration load to confirm the window is wide enough for real-world cell sag, and that the 3.0s recovery time doesn't feel too sluggish or too twitchy in practice. |
| Cell imbalance monitor (100mV warn, monitor only) | 100mV | Confirmed (software) | **Added 2026-07-31, widened 2026-08-01** (was 50mV). Unit-tested: an artificial spread correctly triggers the warning text, a balanced pack (0mV spread) correctly reports "ok," and the monitor never asserts `capacity_empty`/`relay_cut_request` under any spread (monitor-only by design, verified). The threshold itself is a general industry figure, not tuned against this specific pack's real cell-to-cell variance over time. |
| Overcurrent monitor (150A discharge / 30A charge-regen warn, monitor only) | 150A / 30A | Documented (software-verified logic) | **Added 2026-07-31** (docs/12 finding F2). Unit-tested: elevated current doesn't warn on the first tick (persistence guard confirmed), does warn once sustained past the 5.0s persistence window, never derates/cuts power under any current reading (monitor-only, verified), and the saturation note correctly appears at/near the `0x023` sensor's ±204.7A ceiling. The threshold VALUES are provisional pending real drive-cycle current logging (see `10-open-questions.md` #8) - no cell datasheet exists for this pack, so these are derived from this project's own sensor/charger specs, not a manufacturer limit. |
| Cell data cross-check (150mV max delta vs. `0x020` pack summary, 60s soft/+5s hard) | 150mV, 60/65s | Documented | **Added 2026-08-03**. Unit-tested against synthetic per-cell-vs-pack-summary disagreement; never observed against a real decode fault or sensor discrepancy on actual hardware. |
| Temperature data cross-check (10°F max delta vs. `0x4A7` pack extremes, 60s soft/+5s hard) | 10°F, 60/65s | Documented | **Added 2026-08-04** (docs/13 item 16.2). Unit-tested against synthetic pack-extremes-vs-per-probe disagreement, including that it correctly reports "no data" rather than false-triggering when only `temp_max`/`temp_min` are populated with no individual probes (the state every other existing test in this project uses). Never observed against a real decode fault or real spatial thermal gradient across the pack's 4 sub-packs, so the 10°F threshold itself is unconfirmed. |
| Hard-cut latching (`ManagementEngine._hard_latched`, F8) | N/A (behavior, not a threshold) | Documented (software-verified) | **Added 2026-08-01** (docs/12 finding F8). Unit-tested: a hard cut stays asserted after the triggering reading recovers, `notify_session_start()` clears it, `notify_charge_replug()` clears it (only after a genuine ≥3.0s gap, not a brief dropout), and it does not clear on the reading merely recovering. Never confirmed against a real ignition cycle or charger replug event. |
| Input plausibility validation (`PLAUSIBLE_RANGES`) | on (default) | Documented (software-verified) | **Added 2026-08-01, given a real enable/disable toggle 2026-08-03** (docs/13 item 15.14 - previously always-on with no config). Unit-tested (`tests/test_rz450e_signals.py`, `tests/test_realtime_engine.py`): an out-of-range decoded value is rejected and never written to state when enabled, passes through unfiltered when disabled, and the `input_validation_reject` fault_log entry clears live on disable. Never observed rejecting a genuinely implausible real-hardware reading (bus noise, a marginal sensor) vs. a synthetic test value. |
| Toyota checksum validation (5 confirmed IDs) | on (default) | Documented (software-verified) | **Added 2026-08-03** (docs/13 item 13.5), **given a real enable/disable toggle same day** (docs/13 item 15.15 - previously always-on with no config). Unit-tested against synthetic corrupted frames, including the toggle itself (`tests/test_realtime_engine.py` runs `_ingest_rz_bus()` against a real queued frame with a deliberately-wrong checksum byte and confirms both the reject-when-enabled and decode-when-disabled paths); never observed rejecting a real corrupted frame from real bus noise, nor confirmed it doesn't false-reject genuine real traffic. |
| Charge-start data gate (`require_live_data_to_charge`) | on (default) | Documented (software-verified) | **Added 2026-08-03, reworked same day, session-boundary bug fixed 2026-08-04**. Unit-tested extensively (`tests/test_charge_ramp.py`): blocks the ramp when cell data has never arrived or is only from a previous session, proceeds once data goes live within the current session and stays satisfied even if that data later ages, proceeds immediately when disabled. Never confirmed against real RZ450e hardware that a fresh session actually gets all 96 per-cell voltages populated before a real charge request shows up in practice. |

## Hardware connections (`08-gui-design.md`)

| Item | Status | Notes |
|---|---|---|
| RZ450e single combined adapter (one PCAN carries both logical buses, split by CAN ID) | Documented | User-confirmed as the correct hardware wiring (2026-07-31), but not yet tested against a live adapter — confirm every expected ID (diagnostic/DID traffic and the fast internal-bus broadcasts) actually arrives on the one connection with no ID collisions. |
| Log panel captures connection/shutdown/cut events | Documented (software-verified) | Confirmed via automated smoke test that connect/disconnect, sequencer phase changes, and soft/hard-cut assertions reach the log; not yet observed during a real connect/disconnect cycle. |
| Dashboard bar gauges (input/conversion/output) | Documented (software-verified) | Confirmed the window builds and ticks without error against synthetic data; ranges used for bar scaling (docs/07's `range` metadata) are display-only estimates, not confirmed against real cell/temp extremes yet. |

## Real-time engine (`06-realtime-engine-and-watchdog.md`)

| Item | Status | Notes |
|---|---|---|
| Fixed-period TX holds schedule under RZ450e data gaps | Documented | Needs a test where RZ450e input pauses/lags while confirming Leaf TX timing doesn't hiccup. |
| Known-good startup cache loads and hands off to live data correctly | Documented | Needs a restart test: stop app mid-session, confirm cached values, restart, confirm handoff. |
| Staleness watchdog two-stage escalation | Documented | See above. |
| Start Bridge / Stop Bridge lifecycle (`idle` → `waiting_for_wake` → `startup`, and back to `idle` on Stop) | Documented (software-verified) | **Added 2026-07-31**. Verified end-to-end against a simulated Leaf RX frame: no transmission at all while `idle`/`waiting_for_wake`, `leaf_tx` populates only after a simulated wake frame, and clears again on Stop. Not yet observed against a real Leaf VCM waking the bus. |
| Staleness watchdog false-positive fix (never-connected vs. went-stale) | Confirmed (software) | **Added 2026-07-31**. `counter_stale_age()` previously fell back to session-start for a never-seen counter, so 60-65s of wall-clock time with zero hardware connected falsely triggered a soft-then-hard cut - reproduced directly (rewound timestamps 70s, watchdog fired) and confirmed fixed (same test now reports "ok"). This is a code-logic fix verifiable in software alone; no real-hardware test needed to consider it done. |

## Startup/shutdown (`07-startup-shutdown-plan.md`)

Ported directly from the Leaf project's own confirmed testing (2026-07-30, real ZE1 40kWh) — that
confirmation transfers to this project's *implementation of the same sequence*, but should still
be re-verified once this bridge is the thing actually driving a real Leaf VCM (not the original
Leaf emulator app).

| Item | Status | Notes |
|---|---|---|
| Staged startup sequence against a real Leaf VCM, driven by this bridge | Documented (inherited confirmation) | Re-verify once milestone 1 is running against real hardware. **Note**: the trigger is now genuinely "real Leaf-bus traffic seen after Start Bridge is pressed" (docs/06 section 0), not app-launch timing - re-verify the *trigger* itself, not just the staged timing table, against a real VCM waking the bus. |
| Four shutdown triggers, driven by this bridge | Documented (inherited confirmation) | Same. |
| Fifth trigger: staleness-watchdog-driven shutdown | Documented | New to this project, not inherited — needs its own test. |
| Sixth trigger: bus-silence timeout (30.0s), defensive fallback | Documented | **Added 2026-08-06**, bridge-specific, not inherited — see `docs/10-open-questions.md` #16 for the real bench-test symptom that prompted this (bridge stayed awake ~110s past every other trigger's expected wind-down point, root cause not identified from the capture) and `07-startup-shutdown-plan.md`'s "Sixth trigger" section for the rationale. Needs a re-test to confirm it actually fires as a last resort without being confirmed against the ORIGINAL unexplained case, which remains open. |
| `LB_RefusetoSleep` (0x55B) derived from ignition freshness, forced 1 during winding_down | Confirmed (software) | **Bug found and fixed 2026-07-31** (docs/07): was hardcoded to 0 (always "ignition on") in every frame sent, contrary to Leaf_BMS_Emulator's confirmed real-capture behavior. Unit-tested directly (`tests/test_shutdown_sequencer.py`): 0 while ignition IDs are fresh, 1 once stale or never seen, 1 forced during winding_down regardless of freshness. Not yet observed against a real Leaf VCM to confirm this actually changes sleep behavior on the real car - that's the real-hardware half of this fix. |
| Post-shutdown re-arm requires genuine bus quiet, not a flat timer | Confirmed (software) | **Bug found and fixed 2026-07-31** (docs/07): matches a bug Leaf_BMS_Emulator hit (their rev 20) and fixed (rev 21) after a real capture showed a still-talking VCM instantly re-triggering their wake detector on a flat post-stop timer. Unit-tested directly: simulating continued Leaf-bus traffic right up to the check does NOT re-arm even past the old flat-timer window; a genuinely quiet bus does re-arm. Needs a real-hardware test with an actual VCM that keeps transmitting past a manual power-down to confirm the bridge now correctly waits instead of prematurely re-arming. |

## Charge-request ramp emulation (`03-target-signals-leaf.md`, `06-realtime-engine-and-watchdog.md`)

| Item | Status | Notes |
|---|---|---|
| Charger ramp requires BOTH a real Leaf `0x1F2` request AND RZ450e `charge_permission_input` | Documented | **Added 2026-07-31** (user directive). Unit-tested (`tests/test_charge_ramp.py`): ramp stays inactive with either signal missing, starts at exactly 0.0kW with both present, rate matches the confirmed 2.0kW/s-at-level-7 (halving per level down) formula, and caps at the configured target. Rate/start values themselves are ported from Leaf_BMS_Emulator's own real-hardware confirmation (bit-level diff of every HVBAT ID); the *combination* with RZ450e's interlock is new to this bridge and not yet observed against a real charge session on this specific pack. |
| Charger-stop on permission mismatch (`full_charge_flag`/`charge_limit_kw`/`charger_limit_kw`) | Documented | **Added 2026-07-31**. Unit-tested: Leaf wanting to charge with RZ450e permission withheld forces `full_charge_flag=1`, `charge_limit_kw=0.0`, `charger_limit_kw=-10.0`; confirmed this does NOT fire when the feature is disabled, and does NOT fire when nothing is requesting charge at all (no false stop during ordinary non-charging operation). Not yet observed on real hardware - needs a bench test that withholds RZ450e's own permission relay while the Leaf is actively requesting to charge. |
| Charge-authorization-aware wind-down (`ShutdownSequencer.charge_authorized`) | Documented | **Added 2026-07-31**. Unit-tested (`tests/test_shutdown_sequencer.py`): an authorized active request still holds off wind-down exactly as before; an unauthorized-but-active request starts the existing charge-end wind-down timer instead, and the sequencer does wind down once that timer elapses. Needs a real-hardware test: withhold RZ450e permission while the Leaf keeps sending `0x1F2`, confirm the bridge actually goes back to `waiting_for_wake` rather than staying awake. |
| GUI: Charge Emulation Configurator tab + Dashboard "Charge emulation (ramp)" section | Documented (software-verified) | **Added 2026-07-31** (user follow-up: "we need some data on charging and ramp setting etc on the dashboard"). Confirmed via a live `App()` smoke test exercising all four displayed states (disabled / idle / ramping-active / STOPPED-on-mismatch): both windows' status text updates correctly as `charge_emulation` config, a simulated `0x1F2` request, and `charge_permission_input` are changed live. Not yet observed against a real charge session on real hardware. |

## Signal mapping (`04-signal-mapping.md`)

| Item | Status | Notes |
|---|---|---|
| Current sign inversion | Documented | Both source conventions are independently 100%-validated upstream; the inversion itself needs a dedicated bridge-side test (e.g. confirm LeafSpy shows the correct charge/discharge sign while the RZ450e side shows its own confirmed sign). |
| GIDS/QC capacity derivation formula | Documented | **Formula rewritten 2026-08-08** (docs/16 audit): now usable-capacity-based (`vehicle.usable_capacity_kwh`, spec-sourced default 64.0kWh) x measured SOH, instead of the old gross-capacity x live-pack-voltage formula, which overstated GIDs ~12.5% for this pack. QC ceiling now also capped at `vehicle.qc_max_soc_pct` (default 80%, PROVISIONAL - no DC fast-charge testing done at all yet). Software-verified against the user's own by-hand worked numbers (`tests/test_mapping_engine.py`: 64kWh usable x 94% SOH = 752 GIDs at 100% SOC, exact match). Still needs a real SoC sweep, cross-checked against LeafSpy's GIDS display, before promoting past Documented - the fix is confirmed correct FOR the stated usable-capacity number, not that the number itself is right for this exact pack. |
| Dash SOC% formula (`soc_correction`, `0x59E`): `raw = soc_pct × 2.0` (0-200 = 0-100%) | **Confirmed (real hardware)** | User-confirmed 2026-07-31 directly against their own real Leaf's physical dash display, using this bridge's own RZ450e bench pack - not a lab bench guess or a value carried over from the Leaf project (which left this exact question open). Added as a shipped default mapping tie (`bridge/mapping_engine.py`) and the `leaf_signals.py` slider range/default corrected to match (was an unconfirmed 0-255/241 placeholder, now confirmed 0-200/90). Reference profile: `config/7-31-2026-new-SOC-dash-fix.json`. This is the first fully real-hardware-confirmed *mapping* in this project (as opposed to a management-feature threshold) - promoted straight to Confirmed rather than Documented, since the user directly verified it on the actual dash, not just in software. |
| ChargeBars/CapacityBars raw (`capacity_bars_raw`, `0x5BC`): `raw = capacity_pack1_ah × 0.07` (0-200Ah = 0-14 bars) | **Confirmed (real hardware)** | User-confirmed 2026-07-31 directly against their own real Leaf's physical dash SOH/capacity bar display, same session as the dash SOC% fix above. Added as a shipped default mapping tie (`bridge/mapping_engine.py`); `leaf_signals.py`'s existing 0-15 range/6 default were already consistent with this formula (no correction needed there, unlike soc_correction). Reference profile: `config/7-31-2026-new-SOC-dash-fix-added-SOH-for_helth-bars.json`. |

## Fault history, output clamping, and Dashboard relocation (`06-realtime-engine-and-watchdog.md`, `08-gui-design.md`)

| Item | Status | Notes |
|---|---|---|
| Output range clamping (`leaf_signals.clamp_state`) | Confirmed (software) | **Bug found and fixed 2026-07-31**: frame builders bitmask-pack values (`& 0x3FF` etc.), which WRAPS an out-of-range value instead of saturating it. Reproduced directly (`tests/test_output_clamping.py`): an unclamped `discharge_limit_kw=-5.0` decoded back to 251.0kW (full power) before the fix; a realistic 200Ah/400V pack's derived `qc_full_wh` (80000) legitimately exceeds the documented 51100 max even with no bug at all. Confirmed fixed: both cases now clamp to the documented floor/ceiling. Verifiable in software alone - no real-hardware test needed to consider the clamp logic itself done, though it's worth confirming once a real mapping profile is built that nothing legitimate gets clamped unexpectedly. |
| Fault History logging (`bridge/fault_log.py`) | Confirmed (software) | Added 2026-07-31. Unit-tested (`tests/test_fault_log.py`, `tests/test_management_engine.py`): rising-edge-only counting, auto-clear preserves the count, manual reset zeroes it but a still-active condition re-triggers immediately on the next tick, and save/load round-trips correctly (never restoring `active=True` from disk - re-evaluated live). All 12 tracked conditions plus output-clamp events confirmed to actually feed the log by directly triggering each one. Not yet observed against a real fault during actual bench/vehicle operation. |
| Fault History display, moved into the Dashboard window's right column | Documented (software-verified) | Moved 2026-07-31 (second pass) from beside the Log panel in the main window, per user request, so it sits with the rest of the live status info with no scrolling needed. Confirmed via a full `App()` smoke test: Dashboard opens with the management engine wired in, simulated fault triggers update the row's count/color correctly, and a dynamically-triggered output-clamp event correctly grows a new row. Window resized to 1560x1300 to fit everything - not yet confirmed comfortable on every real monitor size; the window is a normal resizable Toplevel if it needs adjusting. |
| Every battery-management config field is exposed/editable in the GUI | Confirmed (software) | Audited 2026-07-31: every key in `management_engine.default_config()` (bar `enabled`/`extended_mode`, both separately handled) has a matching entry in `gui/panels.py`'s `ManagementPanel.FEATURE_FIELDS` - zero gaps found programmatically. |
